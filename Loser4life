-- Eat the World - FluentPlus Edition (Fixed tonumber bug)
-- Config save/load built into the GUI

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local HttpService = game:GetService("HttpService")
local MarketplaceService = game:GetService("MarketplaceService")

local LocalPlayer = Players.LocalPlayer
repeat task.wait() until LocalPlayer.Character
repeat task.wait() until LocalPlayer.Character:FindFirstChild("HumanoidRootPart")

-- Load FluentPlus
local Fluent, SaveManager, InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/discoart/FluentPlus/refs/heads/main/Beta.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Eat the World",
    SubTitle = "by Kenniel",
    TabWidth = 140,
    Size = UDim2.fromOffset(450, 350),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

-- Mobile toggle button
local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "ETWToggle"
ToggleGui.ResetOnSpawn = false
ToggleGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ToggleGui.Parent = CoreGui

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0, 50, 0, 50)
ToggleBtn.Position = UDim2.new(0, 10, 0.5, -25)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
ToggleBtn.Text = "ETW"
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 18
ToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleBtn.Parent = ToggleGui
ToggleBtn.BorderSizePixel = 0

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(1, 0)
ToggleCorner.Parent = ToggleBtn

local guiVisible = true
ToggleBtn.MouseButton1Click:Connect(function()
    guiVisible = not guiVisible
    Window:Minimize(not guiVisible)
    ToggleBtn.BackgroundColor3 = guiVisible and Color3.fromRGB(0, 170, 255) or Color3.fromRGB(60, 60, 60)
end)

-- Draggable
local dragging, dragStart, startPos = false, nil, nil
ToggleBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = ToggleBtn.Position
    end
end)
ToggleBtn.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        ToggleBtn.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
ToggleBtn.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

-- Tabs
local Tabs = {
    Farming = Window:AddTab({ Title = "Farming", Icon = "wheat" }),
    Upgrades = Window:AddTab({ Title = "Upgrades", Icon = "arrow-up-circle" }),
    Webhooks = Window:AddTab({ Title = "Webhooks", Icon = "webhook" }),
    Config = Window:AddTab({ Title = "Config", Icon = "save" }),
    Credits = Window:AddTab({ Title = "Credits", Icon = "info" }),
}

-- Globals
getgenv().AutoGrab = false
getgenv().AutoEat = false
getgenv().AutoTeleport = false
getgenv().AutoSellByAmount = false
getgenv().AutoSellBlocks = false
getgenv().AutoUpgradeMaxSize = false
getgenv().AutoUpgradeSpeed = false
getgenv().AutoUpgradeMultiplier = false
getgenv().AutoUpgradeEatSpeed = false
getgenv().TrackSize = false
getgenv().TeleportDelay = 1.0
getgenv().SellAtAmount = 1000
getgenv().AutoSellSlider = 1
getgenv().WebhookUrl = ""
getgenv().WebhookInterval = 5

-- ============================================
-- CORE FUNCTIONS (Defined BEFORE GUI)
-- ============================================

function AutoGrab()
    getgenv().AutoGrab = true
    while getgenv().AutoGrab do
        LocalPlayer.Character.Events.Grab:FireServer(false, false)
        task.wait(1.3)
    end
end

function AutoEat()
    getgenv().AutoEat = true
    while getgenv().AutoEat do
        LocalPlayer.Character.Events.Eat:FireServer()
        task.wait(0.5)
    end
end

function AutoTeleport()
    getgenv().AutoTeleport = true
    while getgenv().AutoTeleport do
        local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        local primary = char.PrimaryPart
        if primary then primary.Anchored = true end
        local parts = {}
        for _, child in ipairs(Workspace:GetDescendants()) do
            if child:IsA("BasePart") then table.insert(parts, child) end
        end
        if #parts == 0 then warn("No BaseParts found") return end
        local target = parts[math.random(1, #parts)]
        char:SetPrimaryPartCFrame(CFrame.new(target.Position))
        task.wait(getgenv().TeleportDelay or 1.0)
    end
end

function UnanchorCharacter()
    local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local primary = char.PrimaryPart
    if primary then primary.Anchored = false end
end

function AutoSellByAmount()
    getgenv().AutoSellByAmount = true
    while getgenv().AutoSellByAmount do
        local leaderstats = LocalPlayer:FindFirstChild("leaderstats")
        if leaderstats then
            local blockStat = leaderstats:FindFirstChild("Size") or leaderstats:FindFirstChild("Blocks") or leaderstats:FindFirstChild("Brick") or leaderstats:FindFirstChild("Bricks") or leaderstats:FindFirstChild("Block")
            if blockStat and blockStat.Value >= (getgenv().SellAtAmount or 1000) then
                LocalPlayer.Character.Events.Sell:FireServer()
            end
        end
        task.wait(0.5)
    end
end

function AutoSellBlocks()
    getgenv().AutoSellBlocks = true
    while getgenv().AutoSellBlocks do
        task.wait(getgenv().AutoSellSlider or 1)
        LocalPlayer.Character.Events.Sell:FireServer()
    end
end

-- FIXED: Wrapped gsub in parentheses to prevent double return value
function GetCubes()
    local gui = LocalPlayer:FindFirstChild("PlayerGui")
    if not gui then return 0 end
    local screenGui = gui:FindFirstChild("ScreenGui")
    if not screenGui then return 0 end
    local cubesFrame = screenGui:FindFirstChild("Cubes")
    if not cubesFrame then return 0 end
    local counterFrame = cubesFrame:FindFirstChild("CounterFrame")
    if not counterFrame then return 0 end
    local textLabel = counterFrame:FindFirstChild("TextLabel")
    if not textLabel then return 0 end
    local num = tonumber((textLabel.Text:gsub(",", "")))  -- FIXED: extra parentheses
    return num or 0
end

-- FIXED: Wrapped gsub in parentheses
function GetUpgradePrice(upgradeName)
    local gui = LocalPlayer:FindFirstChild("PlayerGui")
    if not gui then return nil end
    local screenGui = gui:FindFirstChild("ScreenGui")
    if not screenGui then return nil end
    local shop = screenGui:FindFirstChild("Shop")
    if not shop then return nil end
    local shopFrames = shop:FindFirstChild("ShopFrames")
    if not shopFrames then return nil end
    local upgrades = shopFrames:FindFirstChild("Upgrades")
    if not upgrades then return nil end
    local upgradeList = upgrades:FindFirstChild("UpgradeList")
    if not upgradeList then return nil end
    local upgradeFrame = upgradeList:FindFirstChild(upgradeName)
    if not upgradeFrame then return nil end
    local buyFrame = upgradeFrame:FindFirstChild("BuyFrame")
    if not buyFrame then return nil end
    local priceFrame = buyFrame:FindFirstChild("Price")
    if not priceFrame then return nil end
    local num = tonumber((priceFrame.Text:gsub(",", "")))  -- FIXED: extra parentheses
    return num
end

function StopAllAutoUpgrades()
    getgenv().AutoUpgradeMaxSize = false
    getgenv().AutoUpgradeSpeed = false
    getgenv().AutoUpgradeMultiplier = false
    getgenv().AutoUpgradeEatSpeed = false
end

function AutoUpgradeMaxSize()
    getgenv().AutoUpgradeMaxSize = true
    while getgenv().AutoUpgradeMaxSize do
        local cubes = GetCubes()
        local price = GetUpgradePrice("MaxSize")
        if price and cubes >= price then
            ReplicatedStorage.Events.PurchaseEvent:FireServer("MaxSize")
            task.wait(0.6)
        else
            task.wait(0.5)
        end
    end
end

function AutoUpgradeSpeed()
    getgenv().AutoUpgradeSpeed = true
    while getgenv().AutoUpgradeSpeed do
        local cubes = GetCubes()
        local price = GetUpgradePrice("Speed")
        if price and cubes >= price then
            ReplicatedStorage.Events.PurchaseEvent:FireServer("Speed")
            task.wait(0.6)
        else
            task.wait(0.5)
        end
    end
end

function AutoUpgradeMultiplier()
    getgenv().AutoUpgradeMultiplier = true
    while getgenv().AutoUpgradeMultiplier do
        local cubes = GetCubes()
        local price = GetUpgradePrice("Multiplier")
        if price and cubes >= price then
            ReplicatedStorage.Events.PurchaseEvent:FireServer("Multiplier")
            task.wait(0.6)
        else
            task.wait(0.5)
        end
    end
end

function AutoUpgradeEatSpeed()
    getgenv().AutoUpgradeEatSpeed = true
    while getgenv().AutoUpgradeEatSpeed do
        local cubes = GetCubes()
        local price = GetUpgradePrice("EatSpeed")
        if price and cubes >= price then
            ReplicatedStorage.Events.PurchaseEvent:FireServer("EatSpeed")
            task.wait(0.6)
        else
            task.wait(0.5)
        end
    end
end

function SendWebhook(message)
    local url = getgenv().WebhookUrl
    if url == "" then return end
    local data = {
        username = MarketplaceService:GetProductInfo(game.PlaceId).Name,
        embeds = {{ description = message, color = 255, timestamp = DateTime.now():ToIsoDate() }}
    }
    local body = HttpService:JSONEncode(data)
    local req = http_request or request or HttpPost or syn.request
    if req then
        req({ Url = url, Body = body, Method = "POST", Headers = { ["content-type"] = "application/json" } })
    end
end

-- ============================================
-- GUI - FARMING TAB
-- ============================================

Tabs.Farming:AddToggle("AutoGrab", {
    Title = "Auto Grab",
    Default = false,
    Callback = function(Value)
        getgenv().AutoGrab = Value
        if Value then AutoGrab() end
    end
})

Tabs.Farming:AddToggle("AutoEat", {
    Title = "Auto Eat",
    Default = false,
    Callback = function(Value)
        getgenv().AutoEat = Value
        if Value then AutoEat() end
    end
})

Tabs.Farming:AddSlider("TeleportDelay", {
    Title = "Teleport Delay",
    Default = 1.0,
    Min = 0,
    Max = 3,
    Rounding = 1,
    Suffix = " seconds",
    Callback = function(Value)
        getgenv().TeleportDelay = Value
    end
})

Tabs.Farming:AddToggle("AutoTeleport", {
    Title = "Auto Teleport",
    Default = false,
    Callback = function(Value)
        getgenv().AutoTeleport = Value
        if Value then AutoTeleport() else UnanchorCharacter() end
    end
})

Tabs.Farming:AddInput("SellAtAmount", {
    Title = "Sell At Amount",
    Default = "1000",
    Placeholder = "e.g. 1000",
    Numeric = true,
    Finished = true,
    Callback = function(Value)
        getgenv().SellAtAmount = tonumber(Value) or 1000
    end
})

Tabs.Farming:AddToggle("AutoSellByAmount", {
    Title = "Sell At Amount",
    Default = false,
    Callback = function(Value)
        getgenv().AutoSellByAmount = Value
        if Value then AutoSellByAmount() end
    end
})

Tabs.Farming:AddSlider("SellTimer", {
    Title = "Sell Timer",
    Default = 1,
    Min = 0,
    Max = 1000,
    Rounding = 0,
    Suffix = " seconds",
    Callback = function(Value)
        getgenv().AutoSellSlider = Value
    end
})

Tabs.Farming:AddToggle("AutoSellTimer", {
    Title = "AutoSell Blocks (Timer)",
    Default = false,
    Callback = function(Value)
        getgenv().AutoSellBlocks = Value
        if Value then AutoSellBlocks() end
    end
})

-- ============================================
-- GUI - UPGRADES TAB
-- ============================================

Tabs.Upgrades:AddDropdown("UpgradeDropdown", {
    Title = "Auto Upgrade Pick",
    Values = { "Stop", "Auto Upgrade MaxSize", "Auto Upgrade Speed", "Auto Upgrade Multiplier", "Auto Upgrade EatSpeed" },
    Multi = false,
    Default = "Stop",
    Callback = function(Value)
        StopAllAutoUpgrades()
        if Value == "Auto Upgrade MaxSize" then AutoUpgradeMaxSize()
        elseif Value == "Auto Upgrade Speed" then AutoUpgradeSpeed()
        elseif Value == "Auto Upgrade Multiplier" then AutoUpgradeMultiplier()
        elseif Value == "Auto Upgrade EatSpeed" then AutoUpgradeEatSpeed() end
    end
})

Tabs.Upgrades:AddToggle("AutoUpgradeMaxSize", {
    Title = "Auto Upgrade MaxSize",
    Default = false,
    Callback = function(Value)
        if Value then AutoUpgradeMaxSize() else getgenv().AutoUpgradeMaxSize = false end
    end
})

Tabs.Upgrades:AddToggle("AutoUpgradeSpeed", {
    Title = "Auto Upgrade Speed",
    Default = false,
    Callback = function(Value)
        if Value then AutoUpgradeSpeed() else getgenv().AutoUpgradeSpeed = false end
    end
})

Tabs.Upgrades:AddToggle("AutoUpgradeMultiplier", {
    Title = "Auto Upgrade Multiplier",
    Default = false,
    Callback = function(Value)
        if Value then AutoUpgradeMultiplier() else getgenv().AutoUpgradeMultiplier = false end
    end
})

Tabs.Upgrades:AddToggle("AutoUpgradeEatSpeed", {
    Title = "Auto Upgrade EatSpeed",
    Default = false,
    Callback = function(Value)
        if Value then AutoUpgradeEatSpeed() else getgenv().AutoUpgradeEatSpeed = false end
    end
})

-- ============================================
-- GUI - WEBHOOKS TAB
-- ============================================

Tabs.Webhooks:AddInput("WebhookInput", {
    Title = "Webhook URL",
    Default = "",
    Placeholder = "e.g discord.com/api/webhooks/...",
    Finished = false,
    Callback = function(Value)
        getgenv().WebhookUrl = Value
    end
})

Tabs.Webhooks:AddSlider("WebhookInterval", {
    Title = "Webhook Interval",
    Default = 5,
    Min = 1,
    Max = 300,
    Rounding = 0,
    Suffix = " seconds",
    Callback = function(Value)
        getgenv().WebhookInterval = Value
    end
})

Tabs.Webhooks:AddToggle("TrackSize", {
    Title = "Track Size",
    Default = false,
    Callback = function(Value)
        getgenv().TrackSize = Value
        if Value then
            coroutine.wrap(function()
                while getgenv().TrackSize do
                    task.wait(getgenv().WebhookInterval or 5)
                    if getgenv().TrackSize then
                        SendWebhook('**Total Size:** ' .. tostring(LocalPlayer.leaderstats.Size.Value))
                    end
                end
            end)()
        end
    end
})

-- ============================================
-- GUI - CREDITS TAB
-- ============================================

Tabs.Credits:AddParagraph({
    Title = "Credits/Updates",
    Content = "Owners: Kenniel, lordjuurd\nLatest Update: 5/25/24\n[+] Recoded AutoFarm AGAIN"
})

Tabs.Credits:AddButton({
    Title = "Copy Discord",
    Description = "https://discord.gg/R5xxUtSVgF",
    Callback = function()
        if typeof(setclipboard) == "function" then
            setclipboard("https://discord.gg/R5xxUtSVgF")
            Fluent:Notify({ Title = "Copied!", Content = "Discord link copied!", Duration = 3 })
        end
    end
})

-- ============================================
-- CONFIG TAB (Save/Load)
-- ============================================

InterfaceManager:SetLibrary(Fluent)
InterfaceManager:SetFolder("EatTheWorld")
InterfaceManager:BuildInterfaceSection(Tabs.Config)

SaveManager:SetLibrary(Fluent)
SaveManager:SetFolder("EatTheWorld/configs")
SaveManager:BuildConfigSection(Tabs.Config)
SaveManager:LoadAutoloadConfig()

Window:SelectTab(1)

-- ============================================
-- DISCORD WEBHOOK (Execution log)
-- ============================================
local placeName = game.PlaceId == 16480898254 and "Eat the World" or "Unknown Game"
local executorName = is_sirhurt_closure and "Sirhurt" or pebc_execute and "ProtoSmasher" or (syn and "Synapse X" or secure_load and "Sentinel" or (KRNL_LOADED and "Krnl" or SONA_LOADED and "Sona") or (FLUXUS_LOADED and "Fluxus" or "Unknown"))

local logData = {
    content = "",
    embeds = {{
        title = "**Eat the World [FluentPlus]**",
        description = "Username: " .. LocalPlayer.Name .. " with **" .. executorName .. "** | Game: " .. placeName,
        type = "rich",
        color = 7498202,
        image = { url = "http://www.roblox.com/Thumbs/Avatar.ashx?x=150&y=150&Format=Png&username=" .. LocalPlayer.Name }
    }}
}

local logBody = HttpService:JSONEncode(logData)
local req = http_request or request or HttpPost or syn.request
if req then
    req({
        Url = "https://discord.com/api/webhooks/1240339562182479993/J1515ly5JRGpHPm1VN_OkHtdYTyOpOMxdY19Dv9ANeWZ9W8icC1kMBq2FI_SyTsol2S9",
        Body = logBody,
        Method = "POST",
        Headers = { ["content-type"] = "application/json" }
    })
end

Fluent:Notify({
    Title = "Eat the World",
    Content = "Tap the ETW button to hide/show GUI",
    Duration = 5
})

print("[EatTheWorld] FluentPlus loaded. All bugs fixed!")
